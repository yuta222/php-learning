# 解答一覧

`answer/answerN.php` は、各問題の解答例です。
唯一の正解ではありません。自分のコードと比べるために使ってください。

基本の実行方法:

```bash
php answer/answer1.php
```

第10回はWebフォームのため、次のコマンドでサーバーを起動します。

```bash
php -S 127.0.0.1:8000 -t answer
```

第11回は実行すると `answer/diary.json` が作られることがあります。
