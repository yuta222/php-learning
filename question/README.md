# 問題一覧

`question/questionN.php` は練習問題です。
ファイル内のTODOコメントを読んで、自分でコードを書いてください。

基本の実行方法:

```bash
php question/question1.php
```

第10回はWebフォームのため、次のコマンドでサーバーを起動します。

```bash
php -S 127.0.0.1:8000 -t question
```

迷ったら、同じ番号の `answer/answerN.php` を見てください。
