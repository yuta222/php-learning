# 第18題 解説: 九九の一段

## 考え方

九九は、同じ段に1から9までを順番にかける処理です。
回数が決まっているため、`for` が向いています。

## 手順

1. 表示したい段を `$dan` に入れます。
2. `$i` を1から9まで増やします。
3. `$dan * $i` で答えを計算します。
4. 式と答えを表示します。

## つまずきポイント

`$i++` は `$i` を1増やす書き方です。
`for` の最後に書くことで、1、2、3と順番に進みます。

## もう一度練習

`$dan` を7に変えて、7の段を表示してください。
