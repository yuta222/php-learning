# 第21題 解説: 合格者だけ表示

## 考え方

配列の中から条件に合うものだけを表示する練習です。
点数を1つずつ見て、60点以上かどうかを判定します。

## 手順

1. 点数の配列を作ります。
2. 合格者数を数える変数を0にします。
3. `foreach` で点数を1つずつ取り出します。
4. 60点以上なら表示し、合格者数を1増やします。

## つまずきポイント

人数を数える変数は、繰り返しの前に0で用意します。
条件に合ったときだけ `++` で増やします。

## もう一度練習

合格ラインを70点に変えたら、合格者数がどう変わるか確認してください。
